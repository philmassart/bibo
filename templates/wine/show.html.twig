{% extends 'base.html.twig' %}

{% block title wine.name %}


{% block body %}

    <div class="jumbotron">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    {% if wine.filename %}
                        <img src="{{ vich_uploader_asset(wine, 'imageFile') | imagine_filter('medium') }}"
                             alt="card-img-top" style="width: 100%; height: auto">
                    {% else %}
                        <img src="{{ '/images/wines/empty.jpg' | imagine_filter('medium') }}" alt="card-img-top"
                             style="width: 100%; height: auto">
                    {% endif %}
                </div>
                <div class="col-md-8">
                    <h1>{{ wine.name }} {{ wine.year }}</h1>
                    <h2>{{ wine.appellation.name }}</h2>
                    {# <div class="text-primary" style="font-size: 4rem; font-weight:bold;" >{{ wine.formattedPrice }}</div> #}
                    {# <a href="#" class="btn btn-primary">Contacter???</a>} #}
                </div>
            </div>

        </div>
    </div>
    <div class="container mt-4">

        <p>
            {{ wine.description | nl2br }}
        </p>

        <div class="row">
            <div class="col-md-8">
                <h2>Caractéristiques</h2>
                <table class="table table-striped">


                    <tr>
                        <td>Appellation</td>
                        <td>{{ wine.appellation.name }}</td>
                    </tr>
                    <tr>
                        <td>Couleur</td>
                        <td>{{ wine.color|trans|capitalize }}</td>
                    </tr>
                    <tr>
                        <td>Alcool</td>
                        <td> {{ wine.alcohol|number_format(1, ',', ' ') }} %</td>
                    </tr>
                    <tr>
                        <td>Contenance</td>
                        <td>{{ wine.container.name }}
                            ({{ wine.container.capacity|number_format(1, ',', ' ') }} {{ wine.container.unit }})
                        </td>
                    </tr>
                    <tr>
                        <td>Nombre (pas encore implémenté)</td>
                        <td></td>
                    </tr>
                    <tr>
                        <td>Producteur (pas encore implémenté</td>
                        <td></td>
                    </tr>
                </table>
            </div>

            <div class="col-md-4">

                <h2>Région</h2>
                <ul class="list-group">
                    <li class="list-group-item">{{ wine.appellation.region.name }}</li>
                </ul>
                <h2>Appellation</h2>
                <ul class="list-group">
                    <li class="list-group-item">{{ wine.appellation.name }}</li>
                </ul>
                <h2>Cépages</h2>
                <ul class="list-group">
                    {% for grape in wine.grapes %}
                        <li class="list-group-item">{{ grape.name }}</li>
                    {% endfor %}
                </ul>

            </div>


        </div>
    </div>
{% endblock %}
