{% extends 'base.html.twig' %}

{% block title 'Gérer les appellations'%}

{% block body %}
    <div class="container mt-4">

        <h1>Gérer les appellations</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>

        {% endfor %}

        <table class="table table-striped">
            <thead>
            <tr>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for appellation in appellations %}
            <tr>
                <td>{{ appellation.name }} </td>
                <td>
                    <a href="{{path ('appellation.edit', {id: appellation.id}) }}" class="btn btn-secondary">Editer</a>
                    <form method="post" action="{{ path('appellation.delete', {id: appellation.id}) }}" style="display: inline-block" onsubmit="return confirm('Êtes-vous certain?')">
                        <input type="hidden" name="_method" value="DELETE">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appellation.id) }}">
                        <button class="btn btn-danger">Supprimer</button>
                    </form>
                </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="div text-right">
            <a href="{{ path('appellation.new') }}" class="btn btn-primary">Créer une nouvelle appellation</a>
        </div>
    </div>



{% endblock %}
