{% extends 'base.html.twig' %}

{% block title %}Container{% endblock %}

{% block body %}
    <h1>Container</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ container.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ container.name }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ container.capacity }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ container.unit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('container.index') }}">back to list</a>

    <a href="{{ path('container.edit', {'id': container.id}) }}">edit</a>

    {{ include('container/_delete_form.html.twig') }}
{% endblock %}
