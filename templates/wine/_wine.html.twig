<div class="card mb-4">
  <div class="card-body">
  <h5 class="card-title">
  <a href="{{ path('wine.show', {slug: wine.slug, id: wine.id}) }}">
    {{ wine.name }}
  </a>
  </h5>
  <p class="card-text">{{ wine.appellation }} {{ wine.year}}</p>
  <div class="text-primary" style="font-size:2rem; font-weight: bold;">
  {{ wine.formattedPrice }} €
</div>
  </div>
</div>
