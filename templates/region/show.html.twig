{% extends 'base.html.twig' %}

{% block title %}Région{% endblock %}

{% block body %}
    <h1>Région</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ region.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ region.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('region.index') }}">back to list</a>

    <a href="{{ path('region.edit', {'id': region.id}) }}">edit</a>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
