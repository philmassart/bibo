{% extends 'base.html.twig' %}

{% block title %}Appellation{% endblock %}

{% block body %}
    <h1>Appellation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appellation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ appellation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('appellation.index') }}">back to list</a>

    <a href="{{ path('appellation.edit', {'id': appellation.id}) }}">edit</a>

    {{ include('appellation/_delete_form.html.twig') }}
{% endblock %}
